---
const { title, description } = Astro.props;
import { ViewTransitions, fade } from "astro:transitions";
import IconD from "../../public/IconD.webp";
---

<!-- prettier-ignore -->
<!doctype html>
<html transition:animate={fade({ duration: "0.4s" })} lang="en">
  <head>
    <ViewTransitions />
    <meta charset="UTF-8" />
    <meta
      name="description"
      content={description ??
        "¡Qué tal! Soy Diego, un desarrollador frontend de México. Me apasiona el desarrollo web, especialmente el ecosistema de React. En mi portafolio encontrarás una variedad de proyectos donde exploro las posibilidades de esta tecnología para crear experiencias web innovadoras y funcionales. Me encanta trabajar con React porque me permite simplificar el proceso de desarrollo y crear interfaces de usuario dinámicas. En cada proyecto, busco desafíos que me permitan mejorar mis habilidades y aprender algo nuevo. Si estás interesado en colaborar o simplemente quieres echar un vistazo a mi trabajo, ¡no dudes en ponerte en contacto conmigo! Estoy siempre abierto a nuevas oportunidades y proyectos interesantes. ¡Gracias por visitar mi portafolio y compartir mi pasión por el desarrollo web!"}
    />
    <meta name="viewport" content="width=device-width" />
    <link
      rel="icon"
      type="image/webp"
      href={IconD.src}
      title="Letra d iconos creados por NajmunNahar - Flaticon"
    />
    <meta name="author" content="Diego Peralta" />
    <meta
      name="keywords"
      content="Diego Peralta, Front-end, Desarrollador Front-end, Desarrollo Front-end, Desarrollador Web Front-end, Desarrollo Web Front-end, Portafolio de Desarrollo Web Front-end, Portafolio de Desarrollo Web Diego Peralta, Portafolio de Desarrollo Web Front-end Diego Peralta, React, Javascript, Typescript, Sass, Tailwind, Typescript"
    />

    <meta property="og:title" content="Portafolio Frontend -- Diego Peralta" />
    <meta property="og:description" content="Si estás interesado en colaborar o simplemente quieres echar un vistazo a mi trabajo, ¡no dudes en ponerte en contacto conmigo! " />
    <meta property="og:image" content= {IconD.src} />

    <meta name="generator" content={Astro.generator} />
    <title>{title ?? "Diego Peralta"}</title>
  </head>

  <body>
    <slot />
  </body>
</html>

<style is:global>
  @font-face {
    font-family: "Figtree";
    src: url("/fonts/Figtree-VariableFont_wght.ttf") format("ttf");
    font-style: normal;
    font-display: swap;
  }

  /* Aquí definimos las Custom properties */
  :root {
    /* --blackbg: #0f0f0f; */
    --blackbg: #000;

    /* Tipografía */
    --font-principal: "Figtree", sans-serif;
  }

  /* Reseteamos los margin y paddings de todas las etiquetas */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    vertical-align: baseline;
  }

  /* Evitamos problemas con las imagenes */
  img,
  picture,
  video,
  iframe,
  figure {
    max-width: 100%;
    width: 100%;
    display: block;
  }

  /* Reseteamos los enlaces para funcionar como cajas... */
  a {
    display: block;
    text-decoration: none;
    color: inherit;
    font-size: inherit;
  }

  /* ... excepto los que se encuentran en párrafos */
  p a {
    display: inline;
  }

  /* Quitamos los puntos de los <li> */
  li {
    list-style-type: none;
  }

  /* Configuramos anclas suaves */
  html {
    scroll-behavior: smooth;
  }

  /* Desactivamos estilos por defecto de las principales etiquetas de texto */
  h1,
  h2,
  h3,
  h4,
  h5,
  h6,
  p,
  span,
  a,
  strong,
  blockquote,
  i,
  b,
  u,
  em {
    font-size: 1em;
    font-weight: inherit;
    font-style: inherit;
    text-decoration: none;
    color: inherit;
  }

  /* Evitamos problemas con los pseudoelementos de quotes */
  blockquote:before,
  blockquote:after,
  q:before,
  q:after {
    content: "";
    content: none;
  }

  /* Configuramos el texto que seleccionamos */
  ::-moz-selection {
    background-color: var(--negro);
    color: var(--blanco);
  }
  ::selection {
    background-color: var(--negro);
    color: var(--blanco);
  }

  /* Nivelamos problemas de tipografías y colocación de formularios */
  form,
  input,
  textarea,
  select,
  button,
  label {
    font-family: inherit;
    font-size: inherit;
    hyphens: auto;
    background-color: transparent;
    color: inherit;
    display: block;
    appearance: none;
  }

  /* Reseteamos las tablas */
  table,
  tr,
  td {
    border-collapse: collapse;
    border-spacing: 0;
  }

  /* Evitamos problemas con los SVG */
  svg {
    width: 100%;
    display: block;
    fill: currentColor;
  }

  /* Ancho de la barra de desplazamiento */
  ::-webkit-scrollbar {
    width: 12px;
  }

  /* Color del fondo de la barra de desplazamiento */
  ::-webkit-scrollbar-track {
    background: transparent;
  }

  /* Color del "pulgar" de la barra de desplazamiento */
  ::-webkit-scrollbar-thumb {
    background: #aeaeae;
    border-radius: 6px;
  }

  /* Cambia el color del "pulgar" cuando se pasa el mouse sobre él */
  ::-webkit-scrollbar-thumb:hover {
    background: #008bfd;
  }

  body {
    min-height: 100vh;
    font-size: 100%;
    hyphens: auto;
    font-smooth: always;
    font-family: var(--font-principal);
    background-color: var(--blackbg);

    position: relative;
  }

  body::before {
    content: "";
    width: 100%;
    height: 100%;

    position: absolute;
    z-index: 0;
    top: 0;

    /* background-color: rgba(0, 0, 0, 0);
opacity: 0.1;
background-image: radial-gradient(#757575 0.8px, rgba(0, 0, 0, 0) 0.8px);
background-size: 16px 16px; */

    background-color: rgba(0, 0, 0, 0);
    opacity: 0.09;
    background-image: radial-gradient(#757575 0.75px, transparent 0.75px),
      radial-gradient(#757575 0.75px, rgba(0, 0, 0, 0) 0.75px);
    background-size: 30px 30px;
    background-position:
      0 0,
      15px 15px;
  }
</style>